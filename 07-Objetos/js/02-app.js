const producto = {
    nombre: 'Monitor de 20',
    precio: 300,
    disponible: false
}
// accedemos a la propiedad precio con el punto
console.log(producto.precio);

// accedemos a la propiedad precio con las llaves
console.log(producto['nombre']);